import React from 'react';

interface Props {
    index: number,
    active: boolean,
    done: boolean
}

export default function QuestionButton({index, active, done}: Props) {
    return (
        <div>

        </div>
    )
}
